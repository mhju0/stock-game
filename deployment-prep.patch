diff --git a/backend/app/main.py b/backend/app/main.py
index e372cfb..8e93a69 100644
--- a/backend/app/main.py
+++ b/backend/app/main.py
@@ -16,8 +16,17 @@ from app.routes.stocks import router as stocks_router
 from app.services.market_service import schedule_refresh
 from app.services.snapshot_service import take_snapshot
 
+import os
+
 models.Base.metadata.create_all(bind=engine)
 
+# CORS: allow localhost for dev, plus any production frontend URL
+ALLOWED_ORIGINS = [
+    "http://localhost:5173",
+]
+if os.environ.get("FRONTEND_URL"):
+    ALLOWED_ORIGINS.append(os.environ["FRONTEND_URL"])
+
 
 async def snapshot_loop():
     while True:
@@ -64,7 +73,7 @@ app = FastAPI(title="Stock Game API", lifespan=lifespan)
 
 app.add_middleware(
     CORSMiddleware,
-    allow_origins=["http://localhost:5173"],
+    allow_origins=ALLOWED_ORIGINS,
     allow_credentials=True,
     allow_methods=["*"],
     allow_headers=["*"],
diff --git a/backend/requirements.txt b/backend/requirements.txt
index fe12f7e..4bdba07 100644
--- a/backend/requirements.txt
+++ b/backend/requirements.txt
@@ -1,5 +1,6 @@
 fastapi
 uvicorn
+gunicorn
 sqlalchemy
 yfinance
 requests
\ No newline at end of file
diff --git a/frontend/vercel.json b/frontend/vercel.json
new file mode 100644
index 0000000..0d199b2
--- /dev/null
+++ b/frontend/vercel.json
@@ -0,0 +1,5 @@
+{
+  "rewrites": [
+    { "source": "/(.*)", "destination": "/index.html" }
+  ]
+}
